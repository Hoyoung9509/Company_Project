apiVersion: v1
kind: Service
metadata:
  name: company-app
  namespace: company
spec:
  type: NodePort
  selector:
    app: company-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
